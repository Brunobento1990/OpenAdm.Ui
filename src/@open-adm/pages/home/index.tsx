export function HomePage() {

    //const { get } = useApi();
    //const [home, setHome] = useState<IHome>();

    //async function init() {
    //    try {
            // var response = await get("home/list") as IHome;
            // setHome(response);
    //    } catch (error) {

    //    }
    //}

    //useEffect(() => {
    //    init();
    //}, [])

    return (
        <>
            
        </>
    )
}
